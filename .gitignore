node_modules
dist
.vercel
.DS_Store
*.log
vite.config.js.timestamp-*.mjs

# Environment files (API keys, secrets)
.env
.env.*
!.env.example

# Downloaded Census boundary files (rebuilt by scripts/build-map.mjs)
data/cache/

# Daily Absentee List files (voter-level data). NEVER commit these.
data/dal/
